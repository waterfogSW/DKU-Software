• x == (int)(float) x (거짓, 정수형으로 변환될때 소수점이하의 숫자가 버려진다)
• x == (int)(double) x (거짓, 정수형으로 변환될때 소수점이하의 숫자가 버려진다)
• f == (float)(double) f (거짓, 더 작은 범위의 자료형으로 변환되어 데이터 일부가 손실된다)
• d == (double)(float) d (참)
• f == -(-f);  (참)
• 2/3 == 2/3.0 (거짓, 2/3은 정수형에서의 연산으로 0의 값이 반환되고 2/3.0은 더 큰 float 형으로 형변환되어 연산되어 실수값이 나온다)
• d < 0.0 ⇒ ((d*2) < 0.0) (참)
• d > f ⇒ -f > -d (참)
• d * d >= 0.0 (참)
• (d+f)-d == f (참)